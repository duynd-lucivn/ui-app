class ItemModel {
  // class constructor
  ItemModel(this.id, this.url, this.title, this.description);

  // class fields
  final int id;
  final String url;
  final String title;
  final String description;
}